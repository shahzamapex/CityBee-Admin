// Standalone public layout for the login page (no admin sidebar).
export const metadata = {
  title: 'Sign in — CityBee Admin',
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
